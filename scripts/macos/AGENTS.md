# macOS Bootstrap implementation rules

Writer MITCHELL; independent IVA is a separate channel. These instructions extend Windows-only AGENTS for this subtree.
Bash 3.2-compatible syntax; fixed Homebrew IDs; no eval, security bypass, curl-pipe installer, forced relinking, profile rewrites or system sudo.
Plan/Verify issue detection commands only. Treat CLI errors as errors, never as empty inventory or successful installation.
Keep Apple CLT/Homebrew first setup and Xcode/SDK license/signing steps user-controlled. Stock prefixes only; Rosetta refused.
No real installer logs, credentials or private paths in repository evidence. CI fixtures are not clean-Mac acceptance.
Read docs/macos/WORK_PLAN_v0.2.md before changes. Preserve Windows tracked blobs except the README navigation update.
